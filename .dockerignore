# RStudio and system files
.Rhistory
.Rdata
.Rproj.user
.DS_Store

# Host libraries/caches must never enter a Linux image.
renv/*
!renv/activate.R
!renv/settings.json

# Git and editor files
.git
.gitignore
.gitattributes
*.swp
*.swo
.idea/
.vscode/

# Git ignore rules do not protect Docker build contexts.
.env
.env.*
!.env.example
.Renviron
.Rapp.history
tests/e2e/node_modules/

# Data and output
data/
# Exception: committed program→dept map required by load_funcs() at load time,
# so the CI test gate can run the suite inside the image without mounted data.
!data/program_map.qs
output/
app_cache/
